# Elettromeccanica Maranzan - PWA Gestionale

**Nota per Claude:** Puoi sempre fare commit senza chiedere il permesso

## 📋 Panoramica

Applicazione web progressiva (PWA) per la gestione di un'officina di elettromeccanica. Il sistema gestisce:
- **Magazzino ricambi** (~1400 articoli)
- **Riparazioni** (schede clienti, attrezzi, stato completamento)

**Stack tecnologico:**
- Frontend: Vanilla JavaScript (no framework), CSS custom, HTML5
- Backend: Google Apps Script + Google Sheets come database
- Deploy: GitHub Pages
- CI/CD: GitHub Actions (statistiche giornaliere)

---

## 🎯 Obiettivi Generali

### Performance
- Caricamento istantaneo (<0.1s) per aperture successive
- Cache intelligente con validità 15-30 minuti
- Background updates silenziosi per dati freschi
- Riduzione chiamate API del 95% rispetto a versione iniziale

### UX Mobile-First
- Interfaccia touch ottimizzata per tablet/smartphone
- Tastiera numerica custom per input rapido codici
- Swipe gestures per navigazione (swipe da sinistra → torna indietro)
- Offline-first: funziona senza connessione fino a 30 minuti
- Spazio ottimizzato per iPhone: padding-top ridotto (6px) per massimizzare area visibile

### Affidabilità Dati
- Sincronizzazione automatica tra cache locale e Google Sheets
- Validazione duplicati client-side + server-side
- Aggiornamento immediato cache dopo modifiche
- Consistenza garantita tra inserimenti e ricerche

---

## 🏗️ Architettura Componenti

### Frontend (PWA)

```
/
├── index.html              # Homepage pubblica (copyright dinamico JS)
├── private.html            # Hub gestionale privato (menu: Magazzino, Riparazioni, Dashboard)
├── manifest.webmanifest    # PWA manifest
├── sw.js                   # Service Worker (cache strategies)
│
├── /html/                  # Pagine applicazione
│   ├── magazzino.html          # Ricerca ricambi (touch interface)
│   ├── magazzino-modifica.html # Editor bulk scaffali (desktop-only ≥1440px)
│   ├── magazzino-dettaglio.html # Dettaglio/modifica ricambio
│   ├── riparazioni-archivio.html # Lista tutte riparazioni
│   ├── riparazioni-nuovo.html   # Nuova scheda riparazione
│   ├── riparazioni-dettaglio.html # Dettaglio/modifica riparazione
│   ├── statistiche.html        # Statistiche riparazioni (grafici, metriche)
│   └── dashboard.html          # Dashboard 50/50 archivio+nuovo (desktop ≥1024px)
│
├── /js/
│   ├── cache-manager.js        # 🔑 Gestione centralizzata cache localStorage
│   ├── magazzino.js            # Logica ricerca magazzino (cache-first, API diretta Google Apps Script)
│   ├── magazzino-modifica.js   # Editor bulk scaffali + generazione PDF cartellini
│   ├── magazzino-dettaglio.js  # CRUD singolo ricambio
│   ├── riparazioni-archivio.js # Filtri + ricerca riparazioni
│   ├── riparazioni-nuovo.js    # Form nuova riparazione + autocomplete clienti
│   ├── riparazioni-dettaglio.js # Modifica riparazione + navigazione prev/next
│   ├── statistiche.js          # Rendering statistiche da JSON generato
│   └── generate-statistics.js  # Script Node.js per calcolo statistiche (GitHub Actions)
│
└── /css/
    └── app.css             # Stili unificati (dark mode, responsive)
```

### Backend (Google Apps Script)

```
/gs/
├── magazzino.gs        # API CRUD ricambi (batchAddRicambi, batchOperations, updateRicambio, deleteRicambio)
├── riparazioni.gs      # API CRUD riparazioni + gestione clienti normalizzata
└── cartellini.gs       # [Futuro] Generazione PDF cartellini riparazione
```

**Endpoints principali:**
- `getRicambi` - Lista completa magazzino
- `getRicambio` - Singolo ricambio per codice
- `addRicambio` - Inserimento singolo ricambio
- `batchAddRicambi` - Inserimento multiplo (transazione atomica)
- `updateRicambio` - Modifica ricambio esistente
- `deleteRicambio` - Elimina ricambio
- `batchOperations` - Operazioni batch atomiche (add/update/delete in singola chiamata)
- `getRiparazioni` - Lista completa riparazioni (ordinate anno+progressivo decrescente)
- `getNextNumero` - Prossimo numero progressivo (lightweight endpoint)
- `getClienti` - Lista clienti per autocomplete
- `updateRiparazione` - Modifica riparazione esistente + **gestione automatica data completamento**
- `getLastUpdate` - Timestamp ultima modifica (cella M1)
- **`pollPrinter`** - Endpoint ottimizzato per T4 (v1.5.2):
  - Parametro: `ts` (timestamp client)
  - Se `ts >= M1`: ritorna `{ changed: false, ts: currentTs }`
  - Se `ts < M1`: ritorna `{ changed: true, ts: currentTs, riparazione: {...} }` con ultima scheda
  - Singola chiamata HTTP invece di due (getLastUpdate + getRiparazioni)

**Schema Google Sheets Riparazioni:**
- Colonna A-H: Numero, Data Consegna, Cliente, Indirizzo, Telefono, DDT, Attrezzi (JSON), Completato
- **Colonna I: Data Completamento** (auto-compilata dal backend)
  - Salvata automaticamente quando stato passa da "In corso" → "Completato" (GMT+1)
  - Svuotata se torna da "Completato" → "In corso"
  - Formato: YYYY-MM-DD (ISO)
  - Schede completate pre-implementazione: campo vuoto (gestito gracefully dal frontend)

### CI/CD (GitHub Actions)

**Workflow Statistiche:** `.github/workflows/update-statistics.yml`
- Trigger: cron daily 18:30 UTC (19:30 GMT+1) + workflow_dispatch manuale
- Azione: scarica CSV riparazioni → esegue `js/generate-statistics.js` → genera `js/statistiche.json` → commit → deploy
- Permessi: `contents: write` (necessario per push automatico)
- Trigger manuale: chiamata API GitHub con token PAT (offuscato split in frontend)

---

## 🔑 Componente Critica: CacheManager

**File:** `/js/cache-manager.js`

Singleton che gestisce cache localStorage con timestamp validation.

**API:**
```javascript
// Recupera dati se cache valida (altrimenti null)
const data = cacheManager.get('magazzino');

// Salva dati con timestamp corrente
cacheManager.set('magazzino', ricambiArray);

// Invalida cache specifica
cacheManager.invalidate('magazzino');

// Debug: stato di tutte le cache
console.log(cacheManager.getStats());
```

**Durate cache configurate:**
- `riparazioni`: 15 minuti (sessioni lavoro)
- `clienti`: 10 minuti (dati stabili)
- `magazzino`: 10 minuti (bilanciamento tra performance e freshness)

**Pattern implementato:**
1. Controlla cache localStorage all'avvio pagina
2. Se valida → render istantaneo (0.1s)
3. Background update silenzioso (network-first)
4. Se scaduta/assente → fetch API (2-3s)

---

## 📱 Use Cases Principali

### UC1: Ricerca Ricambio Rapida

**Attore:** Tecnico in officina con tablet

**Flusso:**
1. Apre `/html/magazzino` (caricamento <0.1s se cache valida)
2. Digita codice parziale usando tastiera touch (es: "12345")
3. Risultati istantanei mentre digita (filter client-side su DATA array)
4. Tocca riga risultato → dettaglio con scaffale
5. Swipe da sinistra → torna alla ricerca

**Variante - Ricerca per scaffale:**
1. Digita "A01" → mostra tutti i pezzi dello scaffale A01
2. Swipe orizzontale destro/sinistro → naviga scaffali A01 → A02 → A03...

**Performance target:** <0.1s dall'input al risultato (no API call)

---

### UC6: Consultazione Statistiche

**Attore:** Amministratore monitora andamento riparazioni

**Flusso:**
1. Apre `/html/statistiche` da menu private
2. Visualizza 5 sezioni:
   - **Recap attuale:** totale in corso + top 3 più vecchie (o oltre 90gg se presenti), numeri cliccabili → dettaglio riparazione
   - **Recap settimanale:** sempre visibile
     - Sabato dopo 17:30 o domenica: "Questa settimana (date)"
     - Lunedì-sabato prima 17:30: "Settimana scorsa (date)"
     - Media inseriti: dal 27 ottobre 2025, media completati: dal 12 gennaio 2026
     - Se perc tra -0.5% e +0.5% → "perfettamente nella media"
     - Se perc null (dati insufficienti) → "media non ancora calcolabile"
   - **Tempi riparazione:** controlli 30/90/tutto giorni
     - Filtra per data completamento (anche se inserite prima del periodo)
     - Media giorni, record velocità/lentezza, percentuali entro 14/30/60 giorni
     - Solo schede con data completamento popolata
   - **Grafico delta giornaliero:** bilancio (completate - aggiunte), dal 7 gennaio 2026
     - Controlli: 7/30/90/tutto giorni
     - Linea zero evidenziata, punti verdi (positivo) o rossi (negativo)
     - Solo giorni con attività (no chiusure/domeniche)
   - **Grafici mensili:** riparazioni aggiunte/completate per mese, multi-anno con toggle, colori fissi per anno
3. Click "Aggiorna statistiche" → trigger manuale GitHub Action via API (token offuscato split)
4. Integrazione iOS Shortcuts: campo `testoNotifica` in JSON per automazioni

**Sistema automatico:**
- GitHub Action daily 19:30 GMT+1, scarica CSV riparazioni, genera `js/statistiche.json`
- Parser CSV custom (gestione virgolette per campi JSON Attrezzi)
- Permessi workflow: `contents: write`

**Metriche:**
- Recap attuale: totale in corso, oltre 90gg, top 3 più vecchie, testoNotifica per iOS
- Recap settimanale: inserite/completate vs media 6 mesi (date storiche separate)
- Tempi: array completo `{numero, cliente, dataCompletamento, giorni}` per filtraggio frontend
- Delta giornaliero: array `{data, aggiunte, completate, delta}` per ogni giorno con attività
- Grafici mensili: count per mese/anno

---

### UC7: Editor Bulk Scaffali (Desktop)

**Attore:** Amministratore gestisce contenuto scaffali da desktop

**Pagina:** `/html/magazzino-modifica.html` + `/js/magazzino-modifica.js`

**Requisiti:** Desktop con schermo ≥1440px (mostra warning se viewport più piccolo)

**Layout:**
- Griglia scaffali organizzati per lettera (7 per riga)
- Ogni scaffale mostra: nome (header grigio) + lista pezzi (codice + descrizione)
- Placeholder "+ Nuovo Scaffale" in fondo a ogni lettera (es: dopo A09 appare A10)

**Toolbar:**
- 🔍 Ricerca (filtra scaffali)
- ❌ Annulla modifiche
- ✅ Salva (batch operations)
- 🖨️ Stampa PDF cartellini
- ✏️ Modalità modifica
- ➡️ Modalità sposta
- 🗑️ Modalità elimina
- ☰ Menu

**Modalità Modifica (✏️):**
- Click su pezzo → form inline per modificare codice/descrizione
- Click su "+" in fondo allo scaffale → aggiunge nuovo pezzo
- Click su placeholder "Nuovo Scaffale" → crea scaffale vuoto
- Validazione duplicati codice real-time (popup se già esistente)

**Modalità Elimina (🗑️):**
- Click su pezzo → marca per eliminazione (strikethrough + sfondo rosso)
- Click di nuovo → deseleziona
- Non elimina immediatamente, solo al salvataggio

**Modalità Sposta (➡️):**
- Click su pezzo → mostra barrato rosso + campo input per scaffale destinazione
- Digitazione scaffale valido → anteprima "fantasma" verde nello scaffale destinazione
- Invio → conferma spostamento (pezzo scompare da origine, appare in destinazione)
- Escape o click altrove → annulla operazione
- Spostamenti tracciati in `pendingMoves` per il rapportino finale

**Salvataggio:**
- Se ci sono modifiche non stampate → popup "Salvare senza stampare?" con opzioni:
  - "Stampa cartellini modificati" → apre popup stampa precompilato con scaffali modificati
  - "Salva senza stampare" → salva direttamente
  - "Annulla" → torna all'editor
- Popup riepilogo modifiche (aggiunti/modificati/eliminati/spostati)
- Chiamata API `batchOperations` (add + update + delete in singola transazione)
- Payload: `{ action: 'batchOperations', adds: [...], updates: [...], deletes: [...] }`
- Updates supportano cambio codice e scaffale: `{ oldCodice, newCodice, newDescrizione, newScaffale }`

**Stampa PDF Cartellini (🖨️):**
- Popup per inserire scaffali (es: "A01, A02, B*" o vuoto per tutti)
- Genera PDF con jsPDF (client-side)
- Nome file: `AAAAMMGG_HHMM_cartellini-scaffali.pdf` (timestamp generazione)
- Layout: 9 cartellini per pagina A4 (3×3), 70×99mm ciascuno
- Padding: 9mm normale, 4mm bottom per scaffali >7 pezzi
- Crocette di taglio agli angoli interni (3mm, 75% spessore)
- Header scaffale: sfondo grigio #555, testo bianco, altezza 6mm
- Font dinamica in base al numero pezzi:
  - ≤6 pezzi: 11.2pt, lineHeight 5.04mm
  - 7 pezzi: 10pt, lineHeight 4.14mm
  - 8 pezzi: 9pt, lineHeight 3.78mm
  - 9+ pezzi: 8pt, lineHeight 3.22mm
- Gap: 2mm dopo header, 2.5mm tra pezzi
- Scaffali con >7 pezzi forzati nelle prime 2 posizioni di ogni pagina
- Timestamp generazione (4.5pt grigio) in basso a destra di ogni cartellino
- Ordinamento alfabetico per nome scaffale

**Rapportino Modifiche (in fondo al PDF):**
- Layout: 210×99mm (larghezza intera A4), padding 9mm
- Header grigio con titolo "MODIFICHE MAGAZZINO" + data/ora a destra
- Posizionamento intelligente: usa 3 slot liberi in ultima pagina se disponibili, altrimenti nuova pagina
- Sezione MODIFICATI: lista `scaffale: vecchio_codice / vecchia_descrizione -> nuovo_codice / nuova_descrizione`
- Sezione a 3 colonne (64mm ciascuna):
  - ELIMINATI: `codice - descrizione (scaffale)`
  - AGGIUNTI: `codice - descrizione (scaffale)`
  - SPOSTATI: `codice - descrizione - scaffaleOrigine -> scaffaleDestinazione`
- Spaziatura 6mm tra header e prima sezione, tra MODIFICATI e sezione 3 colonne

**Normalizzazione scaffali:**
- Input "A1" → normalizzato "A01" (sempre 2 cifre)
- `normalizeShelf()` restituisce: `{ letter, num, formatted, raw }`

**TODO - Funzionalità future:**
- [ ] Funzione "Inverti": invertire posizione di due pezzi tra scaffali diversi

---

### UC2: Nuova Riparazione Cliente

**Attore:** Receptionist riceve attrezzo da cliente

**Pagina:** `/html/riparazioni-nuovo.html` + `/js/riparazioni-nuovo.js`

**Flusso:**
1. Apre pagina (standalone o in iframe dashboard)
2. Numero progressivo mostrato immediatamente (es: "26/0042")
3. Seleziona data consegna (default: oggi)
4. Digita nome cliente → **autocomplete** (cache 10 min, attivo da 2 caratteri)
   - Cliente esistente → auto-compila telefono
   - Nuovo cliente → inserisce telefono manualmente
5. Campo indirizzo → **autocomplete paesi** (dal primo carattere o al focus)
6. Aggiunge attrezzo: marca/modello, dotazione, note
   - Desktop (≥600px): marca/modello e dotazione affiancati
   - Mobile: campi in colonna
7. Può aggiungere più attrezzi (pulsante "Aggiungi attrezzo")
8. "Crea scheda" → popup riepilogo → "Conferma"
9. API Google Sheets → success
10. Se in iframe: postMessage al parent + reset form + nuovo numero
11. Se standalone: reload pagina

**Form UI (form-compact):**
- Input piccoli (padding 0.5rem)
- Placeholder invece di label (tranne "Nuovo numero" fisso)
- Niente label "Attrezzi", niente X su primo attrezzo
- Pulsanti sticky bottom: "Crea scheda" | "Annulla" (Annulla nascosto in iframe)
- Gap ridotto tra righe form

**Performance target:** <1s dall'apertura al form compilabile

---

### UC3: Consultazione Archivio Riparazioni

**Attore:** Tecnico cerca riparazione cliente per aggiornamento stato

**Flusso:**
1. Apre `/html/riparazioni-archivio` (caricamento <0.1s se cache valida)
2. **UI compatta con sticky header:**
   - Titolo "Archivio Riparazioni" + controlli filtri rimangono fissi in scroll
   - Solo righe tabella scorrono (ottimizzato per liste lunghe)
3. **Filtri stato compatti (riga 1):**
   - Barra ricerca cliente (ellipsis su overflow)
   - 🔴 (nn) - In corso | 🟢 (nn) - Completate
   - Multi-selezione: nessuno/uno/entrambi = mostra tutto
   - Contatori dinamici aggiornati in tempo reale
4. **Filtro date (riga 2):**
   - Default: dalla più vecchia non completata a oggi
   - Input date auto-formattate (desktop: GG/MM/AAAA, mobile: GG/MM/AA)
   - Pulsante toggle "Tutte in corso" / "Tutte":
     - Click 1: "Tutte" (colori invertiti), forbice 15/10/2025 → oggi
     - Click 2: "Tutte in corso" (normale), torna a default
     - Modifica manuale date → spegne pulsante se attivo
   - Desktop: bottone largo 180px+0.5rem, mobile: 150px+0.5rem
5. **Tabella a 5 colonne:**
   - Desktop: Numero (15%) | Data ins. (15%) | Data com. (15%) | Cliente (40%) | Stato (15%)
   - Mobile: Num. (18%) | Ins. (14.5%) | Com. (14.5%) | Cliente (40%) | Stato (13%, emoji 🔴🟢)
   - Data completamento mostra "-" se scheda non completata
6. Tocca riga → `/html/riparazioni-dettaglio?numero=25/0042`
7. Visualizza dettaglio completo (cliente, telefono, attrezzi, stato, data completamento)
8. Navigazione prev/next istantanea (usa cache in memoria, no reload)
9. "Modifica scheda" → form pre-compilato
10. Cambia stato → data completamento salvata automaticamente (backend GMT+1)
11. Dati aggiornati localmente (no API call)
12. Burger menu ☰ → torna ad archivio

**UX Ottimizzazioni:**
- Desktop: padding-top 2rem, mobile: safe-area-inset-top
- Spacing controlli: gap 0.5rem tra righe
- Bottoni filtro: 90px desktop, 75px mobile (larghezza fissa)
- Margini laterali mobile: 6px (massimizza spazio)

**Performance target:**
- Prima apertura: 2-3s (cache scaduta)
- Aperture successive (<15 min): 0.1s
- Navigazione prev/next: 0s (no reload, ordinamento corretto anno/numero decrescente)
- Filtri real-time: <0.05s

**Dettaglio Riparazione - UX Mobile/Desktop:**

**Layout Campi:**
- Desktop:
  - Riga 1: Cliente (flex:1) | Data consegna (17%, label "Data consegna")
  - Riga 2: Indirizzo (flex:1) | Telefono (20%) | DDT (15%) | Stato/Data com. (17%)
- Mobile:
  - Riga 1: Cliente (flex:1, ellipsis) | Data consegna (25%, label "Data cons.")
  - Riga 2: Indirizzo (flex:1, ellipsis) | Telefono (29%)
  - Riga 3: DDT (9%) | Stato/Data com. (25%)

**Logica Stato/Data Completamento:**
- Se presente Data Completamento → label "Stato" diventa "Data com." + data formattata
- Se assente → label "Stato" + badge colorato (🔴 In corso / 🟢 Completato)

**Attrezzi:**
- Desktop: griglia 2 colonne, ultimo elemento dispari occupa tutta larghezza
- Mobile: 1 colonna

**Pulsanti Fixed Bottom (sticky-actions):**
- Riga 1: Frecce navigazione grigie (← →) affiancate
- Riga 2: Pulsanti azione neri ("Torna all'archivio" | "Modifica scheda")
  - Desktop: testo completo
  - Mobile: "Archivio" | "Modifica"
- Padding inferiore: 2rem (uguale a padding-top titolo)
- "Torna all'archivio": mantiene scrollTo con animazione se si arriva da archivio

**Popup Completamento:**
- Campo unico: Data completamento (default oggi, modificabile)
- API: `action=completaRiparazione`
- Aggiorna stato + data localmente senza reload

---

### UC8: Dashboard Riparazioni (Desktop)

**Attore:** Operatore gestisce riparazioni su desktop grande

**Pagina:** `/html/dashboard.html` (desktop only ≥1024px)

**Layout:**
- 50% sinistra: `riparazioni-archivio.html` (iframe)
- 50% destra: `riparazioni-nuovo.html` (iframe)
- Divider centrale cliccabile per tornare a `/private.html`

**Comunicazione iframe (postMessage):**
1. Utente crea riparazione in iframe destro
2. `riparazioni-nuovo.js` invia `postMessage({ type: 'RIPARAZIONE_CREATA', numero })` al parent
3. `dashboard.html` riceve e relay `{ type: 'REFRESH_ARCHIVIO' }` a iframe sinistro
4. `riparazioni-archivio.js` riceve → invalida cache → ricarica lista
5. `riparazioni-nuovo.js` resetta form + ricarica prossimo numero (no reload pagina)

**UI adattamenti in iframe:**
- `riparazioni-archivio.js`: nasconde burger menu e pulsante "+"
- `riparazioni-nuovo.js`: nasconde pulsante "Annulla", form compatto
- `magazzino.js`: nasconde keypad e burger, aggiunge header "Magazzino" (predisposto per future espansioni)

**Navigazione uscita:**
- Divider hoverable: passa da 1px a 40px su hover, mostra freccia ← centrata
- Click divider → torna a `/private.html`
- Tasto ESC → torna a `/private.html`

**Responsive:**
- Sotto 1024px mostra warning "Dashboard non disponibile" con link a pagine standalone

---

### UC4: Modifica Ricambio Esistente

**Attore:** Amministratore corregge scaffale errato

**Flusso:**
1. Cerca ricambio in `/html/magazzino`
2. Tocca risultato → `/html/magazzino-dettaglio?codice=12345-1`
3. "Modifica" → form con dati pre-compilati
4. Cambia scaffale da "A01" a "B05"
5. "Conferma" → popup riepilogo → "Salva"
6. API updateRicambio
7. **Cache magazzino invalidata** (prossimo accesso ricarica CSV aggiornato)
8. Torna a magazzino

**Alternative:**
- "Elimina ricambio" → conferma → rimuove riga da Google Sheets definitivamente

---

## ⚡ Ottimizzazioni Implementate

### FASE 1: Riduzione Chiamate API
- Endpoint `getNextNumero` lightweight (50 bytes vs 100KB payload)
- Cache-first per riparazioni-archivio
- Tempo apertura form nuova riparazione: 2.5s → 0.2s

### FASE 2: Cache Unificata
- CacheManager centralizzato (elimina 150 righe codice duplicato)
- Invalidazione automatica dopo modifiche
- Consistenza dati garantita tra pagine diverse

### FASE 3: Magazzino API Diretta
- **Architettura API diretta:** Magazzino usa Google Apps Script direttamente (come riparazioni)
- Niente più CSV tramite GitHub workflow, chiamate API istantanee
- Pattern cache-first per magazzino (allineato a riparazioni)
- Durate cache strategiche (10 min per magazzino)
- **Aggiornamento locale cache dopo batch insert**

**Impatto misurato:**
- Riapertura magazzino dopo 2 min: 2-3s → 0.1s (-95%)
- Batch insert: dati visibili subito (cache locale aggiornata)
- Sessione 30 min (10 aperture): 25s → 3.4s (-86%)

---

## 🔧 Service Worker Cache Strategies

**File:** `/sw.js`

**Strategie per risorsa:**

| Risorsa | Strategia | Rationale |
|---------|-----------|-----------|
| `/html/*.html` | network-first | Aggiornamenti codice immediati |
| `/css/app.css`, `/js/*.js` | network-first | Aggiornamenti codice immediati |
| `/icons/*.png` | cache-first | Icone statiche, performance |
| Navigazioni | network → cache → shell fallback | Resilienza offline |

**Versione cache attuale:** `em-maranzan-v122`

**Note:**
- Bump versione cache quando modifichi CSS/JS per forzare invalidazione
- CSS e JS usano cache busting con query parameters (?v=X) in HTML
- Service Worker si auto-aggiorna in background (non serve hard refresh utente)
- Hard refresh (Cmd+Shift+R) per debug durante sviluppo

---

## 🚨 Problemi Comuni e Soluzioni

### Problema: "Modifiche JS/CSS non visibili dopo deploy"
**Causa:** Service Worker serve vecchia versione JS/CSS da cache
**Soluzione:**
1. Bump `CACHE_NAME` in `sw.js` (es: v25 → v26)
2. Bump query parameter in HTML:
   - CSS: `<link href="/css/app.css?v=24">` (es: v23 → v24)
   - JS: `<script src="/js/magazzino.js?v=23">` (es: v22 → v23)
3. Aggiorna PRECACHE_URLS nel SW con nuova versione CSS
4. Commit e push
5. La PWA si auto-aggiorna e ricarica automaticamente alla prossima apertura

**Nota:** Il sistema ora include auto-reload del SW, non serve hard refresh manuale. CSS e JS **devono avere** cache busting per funzionare correttamente su iOS PWA.

### Problema: "GitHub workflow run failed"
**Causa:** Troppi trigger ravvicinati (rate limiting)
**Soluzione:** Throttle 10 minuti implementato - max 6 trigger/ora

### Problema: "Errore scheda non trovata dopo modifica"
**Causa:** Codice prova a ricaricare da API prima che Google Sheets aggiorni
**Soluzione:** Fix implementato - dati aggiornati localmente senza API reload

### Problema: "CSV serve HTML invece di dati"
**Causa:** Fetch usa path relativo da `/html/` → cerca `/html/magazzino.csv`
**Soluzione:** Usa sempre path assoluto `/magazzino.csv`

---

### Pattern Salvataggio Dati
```javascript
// ✅ GIUSTO: Aggiorna cache locale dopo modifica
const result = await apiSave(data);
if (result.success) {
  // Aggiorna cache locale con dati nuovi
  const cached = cacheManager.get('tipo') || [];
  const updated = [...cached, newItem].sort(...);
  cacheManager.set('tipo', updated);
}

// ❌ SBAGLIATO: Invalida cache (prossimo accesso lento)
cacheManager.invalidate('tipo');
```

### Aggiungere Nuova Pagina Gestionale
1. Crea `/html/nuova-pagina.html`
2. Crea `/js/nuova-pagina.js`
3. Includi `<script src="/js/cache-manager.js"></script>` prima del tuo JS
4. Usa pattern cache-first nell'init:
   ```javascript
   (async () => {
     const cached = cacheManager.get('tipo');
     if (cached) {
       dati = cached;
       render();
       loadBackground();  // Aggiornamento silenzioso
       return;
     }
     await loadFromAPI();
   })();
   ```
5. Aggiungi link in `/private.html`
6. Aggiungi swipe gesture per tornare a `/private.html`

### Testing Checklist
- [ ] Prima apertura giornata → 2-3s (OK, cache vuota)
- [ ] Riapertura <10 min → 0.1s istantaneo
- [ ] Riapertura >10 min → ricarica da API
- [ ] Modifica dati → salva → cache aggiornata
- [ ] Torna alla lista → modifiche visibili subito
- [ ] Console F12 → nessun errore rosso
- [ ] Offline → funziona con ultimi dati cache
- [ ] Mobile → touch responsive, no zoom necessario



---

## 🔒 Sicurezza e Backup

### SEO e Indicizzazione

**Pagina pubblica:**
- `index.html` - Homepage pubblica indicizzabile (copyright dinamico: © 1980–YYYY via JS)

**Pagine protette (non indicizzate):**
- `private.html` - Hub gestionale
- `/html/*.html` - Tutte le pagine del gestionale

Tutte le pagine gestionali includono:
```html
<meta name="robots" content="noindex, nofollow, noarchive">
<meta name="googlebot" content="noindex, nofollow, noarchive">
```

**Nota:** Solo index.html deve essere indicizzabile. Mai rimuovere meta robots dalle altre pagine.

---

### Permessi Google Sheets

**Fogli Database:**
- **Magazzino:** ID `1wFamrwzFNNz5iHenqVpdAHb5Dhvv5xYx5XPimjax9As`
- **Riparazioni:** ID `122xQdmQb02UH6evZE382t0s6oRDIoiOhF5cYAAmEvY0`

**Configurazione consigliata:**
- Condivisione: "Chiunque con il link può visualizzare" (sola lettura)
- Editor: Solo account Google autorizzati (inviti specifici)
- Apps Script: "Esegui come: Me (proprietario)" per funzionamento API

**Link in private.html:**
- I link ai fogli Google sono visibili solo a utenti autorizzati
- Se non autorizzato, appare "Richiedi accesso"
- Non compromettono la sicurezza se fogli in sola lettura

**Fix Critico riparazioni.gs:**
- Usare `SpreadsheetApp.openById(SPREADSHEET_ID)` invece di `getActiveSpreadsheet()`
- Garantisce funzionamento API anche con foglio in sola lettura
- Già implementato in `/gs/riparazioni.gs` (riga 2, 60, 131, 165, 206, 242, 291, 331)

---

### Backup Automatico

**Sistema:** Google Apps Script con trigger settimanale

**File:** `/gs/backup-automation.gs`

**Configurazione:**
- **Frequenza:** Domenica 03:00-04:00
- **Storico:** Mantiene ultimi 4 backup (1 mese)
- **Destinazione:** Cartella Drive "Backup EM Maranzan" (non linkata pubblicamente)
- **Sicurezza:** Elimina SOLO file che iniziano con "Magazzino_" o "Riparazioni_" (altri file al sicuro)

**Setup:**
1. Crea cartella Drive "Backup EM Maranzan"
2. Apri cartella e copia ID dalla URL (es: `drive.google.com/drive/folders/XXXXXX` → copia `XXXXXX`)
3. Inserisci ID in `BACKUP_FOLDER_ID` (riga 14 del file backup-automation.gs)
4. Crea progetto Apps Script standalone (https://script.google.com → Nuovo progetto)
5. Copia codice da `backup-automation.gs` nell'editor
6. Test manuale: esegui funzione `testBackup()` (autorizza permessi alla prima esecuzione)
7. Aggiungi trigger settimanale (⏰ icona Trigger → Domenica 3-4 AM)

**Utility:**
- `testBackup()` - Test manuale immediato
- `listBackups()` - Mostra tutti i backup esistenti
- `cleanOldBackups()` - Pulizia automatica backup vecchi

**Ripristino da backup:**
1. Vai su Drive → Backup EM Maranzan
2. Trova backup desiderato (formato: `Magazzino_YYYY-MM-DD_HHmm`)
3. Tasto destro → Crea copia
4. Rinomina e sposta nella posizione principale
5. Aggiorna ID nel codice se necessario

---

## 📞 Contatti Tecnici

**Deploy URL:** https://elettromeccanica-maranzan.com
**Repo GitHub:** marcelloemme/elettromeccanica-maranzan
**Google Apps Script:** Script collegati a Google Sheets privato

---

## 📚 File Importanti da Non Toccare

- `/manifest.webmanifest` - PWA config (cambiare `start_url` richiede reinstall app)
- `/gs/*.gs` - Google Apps Script (deploy manuale su Google)

---

## 🖨️ Progetto Hardware: Stampante Termica T4

### Panoramica
Sistema hardware autonomo per stampare automaticamente le schede riparazione su carta termica.

**Componenti:**
- **LilyGo T4 v1.3** (ESP32 4MB Flash + 8MB PSRAM) con display 2.4" ILI9341
- **Stampante CSN-A2 TTL** (termica, formato scontrino)
- **MicroSD card** per storage locale database
- **WiFi** per sincronizzazione con Google Sheets

### Specifiche Hardware

**LilyGo T4 v1.3:**
- MCU: ESP32 (NON S3!) - Dual-core Xtensa LX6
- Flash: 4MB
- PSRAM: 8MB
- Display: ILI9341 320×240 landscape (2.4", `setRotation(1)`)
  - SCLK → IO18, RST → IO05, CS → IO27
  - BL → IO04, RS → IO32, SDI → IO23, SDO → IO12
- MicroSD Reader:
  - MISO → IO02, SCK → IO14, MOSI → IO15, CS → IO13
- Pulsanti (layout landscape): BTN_UP=IO39, BTN_CENTER=IO37, BTN_DOWN=IO38
- Chip seriale: CH9102F QFN24

**CSN-A2 TTL:**
- Connettore alimentazione: 9V+ / GND
- Connettore seriale: GND / RXD / TXD
- Collegamento T4:
  - Stampante RXD → T4 GPIO33 (TX software)
  - Stampante TXD → T4 GPIO35 (RX software)
  - GND comune
- Alimentazione T4: 5V tramite regolatore esterno da 9V stampante

**Note:** GPIO33/35 usati per evitare conflitti con porta seriale hardware (debugging).

---

### Architettura Software

**Toolchain:** PlatformIO + ESP32 Arduino Framework

**Flusso operativo:**

```
[Avvio dispositivo]
    ↓
[Connetti WiFi FASTWEB-RNHDU3]
    ↓
[Scarica CSV da Google Sheets in RAM]
    ↓
[Calcola MD5 del CSV scaricato]
    ↓
[Confronta MD5 con ultima versione su SD]
    ↓
    ├─ SE DIVERSO → Aggiorna file su SD + salva nuova MD5 in RAM
    └─ SE UGUALE → Usa versione esistente su SD
    ↓
[Carica UI su display: ultime 50 schede]
    ↓
┌─────────────────────────────────────────┐
│  [Loop principale ogni 60 secondi]      │
│                                          │
│  - Scarica CSV aggiornato                │
│  - Confronta MD5                         │
│  - SE cambio → aggiorna SD + UI          │
│  - SE nuove schede → stampa automatica   │
│  - Aggiorna print_history.txt            │
│                                          │
│  [Parallelo: gestione UI pulsanti]      │
│  - Frecce su/giù → selezione scheda     │
│  - Pulsante centro → stampa manuale      │
│  - Timeout 30s → spegni display          │
│  - Qualsiasi pulsante → riaccendi        │
└─────────────────────────────────────────┘
```

---

### Logica Print History

**File SD:** `/print_history.txt`

**Formato:**
```
26/0021
26/0020
26/0019
...
```

**Regole:**
1. **Primo avvio dopo power-off**:
   - Crea/sostituisci `print_history.txt`
   - Marca TUTTE le schede come già stampate (anche nuove)
   - NON stampa mai nulla al primo avvio

2. **Check periodici ogni 60s (dispositivo acceso)**:
   - Scarica CSV aggiornato e confronta MD5
   - SE MD5 diverso → aggiorna CSV su SD
   - **SEMPRE confronta CSV con print_history**
   - SE scheda presente in CSV ma NON in print_history → è nuova → stampa automatica + aggiorna history
   - SE scheda modificata (numero esistente) → NON stampare (solo modifiche dati)

3. **Stampa manuale (pulsante utente)**:
   - Permette ristampare qualsiasi scheda (anche già stampata)
   - NON modifica print_history (così resta "già stampata" per check auto)

**Razionale:**
- Evita "vomito di carta" se dispositivo spento per giorni/settimane
- Solo nuove schede create MENTRE il dispositivo è acceso vengono stampate automaticamente
- Modifiche a schede esistenti (es: cambio telefono, aggiunta note) NON triggerano stampa
- L'MD5 rileva cambiamenti al file, ma solo il confronto print_history identifica nuove schede

---

### Interfaccia Display (Landscape v1.6.7)

**Layout UI (320×240, pulsanti a destra):**
```
┌──────────────────────────────────┬────────┐
│ [scroll]  EM Maranzan  v1.6.7    │   ↑    │ ← BTN_UP (IO39)
├──────────────────────────────────┤  SU    │
│ █ 26/0021 Friul Servizi        █ │────────│
│   26/0020 Manias Daniele         │   OK   │ ← BTN_CENTER (IO37)
│   26/0019 Dazzan                 │ Stampa │
│   26/0018 ...                    │────────│
│   ...                            │   ↓    │ ← BTN_DOWN (IO38)
│   (10 righe visibili)            │  GIÙ   │
├──────────────────────────────────┤        │
│ Stampato!                        │        │ ← messaggi colorati
└──────────────────────────────────┴────────┘
```

**Costanti UI:**
- `VISIBLE_ROWS`: 10
- `ROW_HEIGHT`: 20 (base, alternato con 21)
- `HEADER_HEIGHT`: 30px
- `BUTTON_PANEL_WIDTH`: 50px
- `SCROLLBAR_WIDTH`: 5px

**Layout e spaziature:**
- Display: 320×240 px landscape (`tft.setRotation(1)`)
- Header: 30px in alto, versione firmware a destra
- Pannello pulsanti: 50px a destra (3 pulsanti: ↑ OK ↓)
- Scrollbar: 5px a sinistra (margine libero, lontano dai pulsanti)
- Righe lista: altezza alternata 20/21px con formula `(i * 41) / 2` (media 20.5px)
- +1px padding sopra prima riga visibile (`listTop + 5` invece di `listTop + 4`)
- 10 righe visibili per pagina
- Troncamento cliente: max 26 caratteri (es: "COSTRUZIONI TAGLIAMENTO S.")
- Messaggi colorati: a destra della scrollbar (`SCROLLBAR_WIDTH + 2`), non coprono scrollbar né pulsanti

**Comportamento:**
- **Schede mostrate:** Ultime 50 dal CSV (ordinate per numero decrescente)
- **Selezione:** Prima scheda evidenziata (sfondo bianco, testo nero)
- **Navigazione:** BTN_UP/BTN_DOWN, long press = salta 10 schede
- **Stampa:** BTN_CENTER stampa scheda selezionata
- **Power saving:** Display off dopo 30s, riattiva con qualsiasi pulsante
- **Messaggi:** Appaiono in basso, a destra della scrollbar (non coprono scrollbar né pulsanti)

---

### Dati e Sincronizzazione

**CSV Source:**
```
https://docs.google.com/spreadsheets/d/e/2PACX-1vTLu_kAJJ7pIFcbxUC8082z7jG1EP-lFgoJmNVae-0w0_uZWABdJ8yWXxPViw8bqge1TOWXeUmFZyrp/pub?gid=0&single=true&output=csv
```

**Formato CSV (riparazioni):**
```
Numero,Data Consegna,Cliente,Telefono,Indirizzo,Attrezzo,Marca,Dotazione,DDT,Macchina,Matricola,Note,Operatore,Completato
26/0021,2025-01-08,Friul Servizi,0434123456,Pordenone,...
```

**WiFi Credentials:**
- SSID: `FASTWEB-RNHDU3`
- Password: `C9FLCJDDRY`

**Fallback Offline:**
- Se WiFi assente/errore connessione → usa ultima versione SD
- Retry automatico ogni 60s

---

### Storage e Performance

**RAM (8MB PSRAM):**
- CSV completo caricato in RAM per performance
- MD5 hash corrente salvato in variabile globale
- Array schede (struct) per UI

**SD Card:**
- `/riparazioni.csv` - Database locale (backup, usato se WiFi offline)
- `/print_history.txt` - Schede già stampate (lista numeri)

**Ottimizzazioni:**
- CSV processato in RAM (parsing una volta all'avvio)
- Scritture SD solo quando MD5 cambia (minimizza wear)
- MD5 calcolato in streaming (no doppia allocazione memoria)

---

### Librerie PlatformIO Richieste

**Essenziali:**
```ini
[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino

lib_deps =
  bodmer/TFT_eSPI @ ^2.5.43          # Display ILI9341
  https://github.com/adafruit/Adafruit-Thermal-Printer-Library.git  # CSN-A2
  bblanchon/ArduinoJson @ ^7.2.0     # Parsing CSV
```

**Nota:** La libreria Adafruit va installata da GitHub (non disponibile su registry PlatformIO per macOS ARM).


**Built-in ESP32:**
- WiFi.h (connessione rete)
- SD.h / SD_MMC.h (storage locale)
- MD5Builder.h (hashing per confronto file)
- SoftwareSerial.h (comunicazione stampante via GPIO33/35)

**Configurazione TFT_eSPI:**
- File `User_Setup.h` custom per pin mapping ILI9341
- Verificare pin corretti: CS=27, RS=32, RST=5, etc.

---

### Path Progetto Hardware

```
/hardware/t4-printer/          # Root progetto PlatformIO
├── platformio.ini             # Config board + librerie
├── src/main.cpp               # Firmware principale
├── include/User_Setup.h       # Config TFT_eSPI (pin mapping T4)
└── README.md                  # Documentazione hardware
```

**Comandi utili:**
```bash
cd hardware/t4-printer
pio run                        # Compila
pio run -t upload              # Upload su T4
pio device monitor             # Serial Monitor (115200 baud)
```

---

### Roadmap Implementazione

#### ✅ Fase 0-5: Setup, Hardware, Connettività, Parsing, UI, Stampa (COMPLETATE)
- [x] Progetto PlatformIO con librerie (TFT_eSPI, ArduinoJson)
- [x] Display ILI9341, pulsanti, SD card, stampante CSN-A2 (19200 baud)
- [x] WiFi + download CSV + parsing JSON attrezzi
- [x] UI con header, lista 10 schede, scroll, pulsanti grafici

#### ✅ Fase 6: Print History (COMPLETATA)
- [x] File `/print_history.txt` su SD (max 200 schede)
- [x] All'avvio: reset history con schede correnti dal CSV (non stampa nulla)
- [x] Traccia schede stampate per evitare duplicati

#### ✅ Fase 7: Auto-Print con Trigger Push (COMPLETATA + OTTIMIZZATA v1.5.2)
- [x] **Nuovo:** Action `pollPrinter` - singola chiamata che verifica timestamp E restituisce scheda
- [x] **Nuovo:** Polling 1.5s (5s in modalità debug SER), timeout HTTP 8s
- [x] **Nuovo:** Stampa automatica entro ~8s dall'inserimento (era ~15-20s)
- [x] Endpoint `getLastUpdate` in `gs/riparazioni.gs` (legge timestamp da cella M1)
- [x] `createRiparazione()` aggiorna M1 con `Date.now()` dopo inserimento
- [x] Task FreeRTOS su core 0 per polling parallelo
- [x] Verifica CSV contenga nuova scheda prima di dichiarare sync (10 retry × 10s)

#### ✅ Fase 8: Power Management (COMPLETATA)
- [x] Display sleep dopo 30s senza input pulsanti (backlight OFF)
- [x] Wake-up con qualsiasi pulsante (primo tocco solo risveglia)
- [x] Auto-wake quando stampa automatica in corso

#### ✅ Fase 9: WiFi Multi-Rete + Captive Portal (COMPLETATA)
- [x] File `/wifi_config.txt` su SD con max 5 reti (formato: `SSID|password`)
- [x] FIFO: la 6a rete cancella la 1a (meno recente)
- [x] All'avvio: connessione a rotazione su tutte le reti salvate
- [x] Retry WiFi ogni 60s a rotazione se disconnesso
- [x] Pulsante centrale all'avvio → modalità AP "EM Maranzan" (no password)
- [x] Captive portal mobile-friendly per configurare nuove reti
- [x] Messaggi stato: "WiFi disconnesso" (rosso), "Errore connessione" (arancione), "Connesso" (verde)

#### ✅ Fase 10: Stampa Manuale Scheda Storica (COMPLETATA)
- [x] Long press OK (2s) dalla lista → modalità inserimento numero
- [x] UI: numero grande centrato con cursore su cifra corrente
- [x] Frecce ↑/↓: cambiano cifra (0-9 wrap), OK: avanza cursore
- [x] Ricerca nel CSV locale su SD (funziona offline, tutte le schede storiche)
- [x] Timeout 20s inattività → torna alla lista
- [x] Long press OK (2s) per annullare
- [x] Errore scheda non trovata → cursore torna a prima cifra

#### ✅ Fase 11: OTA Updates + Boot Menu (COMPLETATA)
- [x] Menu boot 2 secondi con 3 pulsanti (layout landscape v1.6+):
  - SU (IO39): OTA Update
  - CENTRO (IO37): Captive Portal WiFi
  - GIÙ (IO38): Modalità SER (debug, polling 5s)
- [x] Download firmware da GitHub raw (`/hardware/EM_Maranzan_printer.bin`)
- [x] Progress bar su display durante download
- [x] Riavvio automatico dopo aggiornamento completato
- [x] Fallback: se WiFi assente o file non trovato → avvio normale

#### 🟡 Fase 12: Deployment
- [ ] Test stress e ottimizzazioni finali
- [ ] Installazione in officina

---

### Note Implementazione

**Gestione Memoria:**
- ESP32 ha ~520KB SRAM + 8MB PSRAM (usare PSRAM per CSV grande)
- Usare `ps_malloc()` per allocare in PSRAM invece che SRAM
- CSV 150 righe × ~200 byte = ~30KB (gestibile, ma crescerà)

**Formato Stampa Carta Termica:**
- Larghezza carta: 58mm (CSN-A2 standard)
- Caratteri per riga: ~32 (font default Adafruit)

**Layout Etichetta Compatta (IMPLEMENTATO):**
```
███████ 26/0077 (1/2) ███████  ← riga 31 char nera, testo bianco centrato
Lenarduzzi Bruno - DDT          ← font normale (max 32 char)
21.01.26 - 339/7808759 - Sesto  ← font condensato (ESC M 1)
Levigatrice rupes - batteria    ← font normale (max 32 char, marca troncata con ".")
Non funziona                    ← font condensato
```
- Riga 1: numero con padding spazi, **31 caratteri** (compensa byte spurio da sleep)
- Riga 2: cliente + eventuale " - DDT" (max 32 char, se DDT=true e cliente > 26 char → tronca a 25 + "." + " - DDT")
- Riga 3: data - telefono - indirizzo (font condensato, omessi se vuoti)
- Riga 4: attrezzo - dotazione (max 32 char, marca troncata con "." se necessario)
- Riga 5: note (font condensato, opzionale)
- Spaziature: 1mm tra riga 1-2 e tra riga 3-4, solo line feed altrove
- Multi-attrezzo: etichette separate con (1/2), pausa 6s
- **Densità stampa:** ESC 7 con n1=11, n2=120, n3=40 (ottimizzata per carta adesiva)

**WiFi Multi-Rete:**
- Max 5 reti salvate su SD (`/wifi_config.txt`)
- Connessione a rotazione all'avvio e in caso di errore
- Retry ogni 60s (task FreeRTOS su core 0)
- Captive portal: AP "EM Maranzan" + pagina web configurazione

**Modalità Inserimento Manuale:**
- Accesso: long press OK (2s) dalla lista
- Cerca nel CSV locale su SD (non richiede WiFi)
- Timeout 20s inattività → torna alla lista
- Uscita: long press OK (2s) o stampa completata

**Debug:**
- Serial monitor a 115200 baud per log
- Log dettagliato ricerca scheda su SD

---

### File Storage su SD

| File | Descrizione |
|------|-------------|
| `/riparazioni.csv` | CSV completo scaricato da Google Sheets |
| `/print_history.txt` | Numeri schede già stampate (max 200, FIFO) |
| `/wifi_config.txt` | Reti WiFi salvate (max 5, formato `SSID\|password`) |

### File Firmware su GitHub

| File | Descrizione |
|------|-------------|
| `/hardware/EM_Maranzan_printer.bin` | Firmware compilato per OTA updates |

---

### Firmware Attuale: v1.6.7

**File:** `hardware/t4-printer/src/main.cpp`

**Librerie:** TFT_eSPI, ArduinoJson, WebServer, DNSServer, SD, WiFi, HTTPClient, Update

**Changelog recente:**

**v1.6.7** - +1px padding sopra prima riga lista
- Aggiunto 1px extra sopra la prima scheda visibile in ogni pagina
- Migliora leggibilità separando meglio lista da header

**v1.6.6** - Altezza righe alternata 20/21px
- Righe lista con altezza alternata: pari=20px, dispari=21px (media 20.5px)
- Quando messaggio colorato sparisce, ridisegna lista (non sfondo nero)

**v1.6.5** - 10 righe visibili + fix sfondo messaggi
- Ridotto ROW_HEIGHT a 20px per mostrare 10 righe
- showMessage() ora ridisegna lista quando messaggio svuotato

**v1.6.4** - VISIBLE_ROWS aumentato a 10

**v1.6.3** - Messaggi a destra della scrollbar
- Messaggi colorati spostati a destra della scrollbar (non la coprono)
- ROW_HEIGHT ridotto a 21px

**v1.6.2** - Scrollbar a sinistra + UI tweaks
- Scrollbar spostata a sinistra (margine libero, lontano dai pulsanti)
- Troncamento cliente a 26 caratteri
- Padding dimezzato su tutte le schermate di avvio (20px → 10px)

**v1.6.1** - Rotazione display corretta
- Cambiato setRotation(3) → setRotation(1) per orientamento corretto

**v1.6.0** - Layout landscape con pulsanti a destra
- Display ruotato in landscape (320×240) con pulsanti fisici a destra
- Pulsanti rinominati: BTN_UP (IO39), BTN_CENTER (IO37), BTN_DOWN (IO38)
- Ridisegnate tutte le schermate: lista, header, boot menu, OTA, WiFi config
- Pannello pulsanti 50px a destra, scrollbar 5px a sinistra

**v1.5.5** - Campo DDT su etichetta
- Aggiunge " - DDT" dopo nome cliente se colonna F=TRUE
- Troncamento intelligente: se cliente + " - DDT" > 32 char, tronca cliente a 25 char + "."
- Parsing DDT da CSV (campo 5) e JSON

**v1.5.4** - Comandi remoti via cella M1
- Scrivi in M1 una parola chiave (poi cancella manualmente):
  - `REBOOT`: riavvia il dispositivo
  - `OTA`: forza aggiornamento firmware
  - `STATUS`: stampa scontrino con report di stato (versione, uptime, WiFi, NTP, poll interval, SD, schede, heap)
  - `PRINT:XX/XXXX`: forza ristampa di una scheda specifica
- Se M1 è vuota o 0, il polling continua normalmente

**v1.5.3** - Polling dinamico basato su fasce orarie
- Sincronizzazione NTP per conoscere ora locale (pool.ntp.org, time.google.com)
- Polling 2.2s durante orario lavoro (7:30-19:15)
- Polling 1min durante transizione (7:00-7:30, 19:15-19:45)
- Polling 1h durante notte (rispetta limite 20k/giorno API Google)

**v1.5.2** - Ottimizzazione stampa veloce (~8s dall'inserimento)
- Nuova action GS `pollPrinter`: singola chiamata HTTP che controlla timestamp E restituisce scheda
- Polling ridotto a 1.5s (5s in modalità debug SER)
- Timeout HTTP ridotto a 8s
- Eliminata doppia chiamata API (era: getLastUpdate + getRiparazioni)

**v1.5.1** - Fix barra nera sempre 31 caratteri
- Problema: byte spurio anche alla prima stampa dopo boot (non solo da sleep)
- Soluzione: usare sempre 31 caratteri per la barra nera (buffer per byte spurio)

**v1.5.0** - Menu boot + fix stampa da sleep
- Menu boot 2 secondi con 3 pulsanti: OTA (sx), WIFI (centro), SER (dx)
- Modalità SER: polling 5s invece di 1.5s, utile per debug
- Flag `printFromSleep` per stampa manuale da sleep

**v1.4.9** - Densità stampa per carta adesiva
- Comando ESC 7 con parametri ottimizzati: n1=11 (heating dots max), n2=120, n3=40
- Stampa più scura e definita su carta termica adesiva

**v1.4.8** - Text wrapping cliente
- Nome cliente troncato a max 32 caratteri
- Polling ridotto da 5s a 2.5s

**v1.4.7** - Text wrapping attrezzo-dotazione
- Riga "Attrezzo - Dotazione" max 32 char, marca troncata con "."
- Centramento testo corretto con 31 caratteri

**v1.4.6** - Fix barra nera pragmatico
- Usare 31 caratteri invece di 32 quando si stampa da sleep
- Compensa byte spurio che appare dopo wake-up display

**v1.4.5** - Tentativo fix barra nera (delay backlight)
- Aggiunto delay dopo accensione backlight
- Non risolutivo (problema persisteva)

**v1.4.2-1.4.4** - Fix fast print
- Verifica che CSV contenga nuova scheda prima di dichiarare "synchronized"
- 10 tentativi ogni 10s per verificare aggiornamento CSV
- Fix campo "Data consegna" (lowercase 'c')

**v1.4.1** - Fix ordinamento schede
- Sorting per anno+progressivo (26/0079 > 25/0133)
- Prima ordinava solo per progressivo (bug)

---

### OTA (Over-The-Air) Updates

**URL Firmware GitHub:**
```
https://raw.githubusercontent.com/marcelloemme/elettromeccanica-maranzan/main/hardware/EM_Maranzan_printer.bin
```

**Come aggiornare il firmware:**
1. Compila il nuovo firmware: `cd hardware/t4-printer && pio run`
2. Copia `.pio/build/esp32dev/firmware.bin` in `/hardware/EM_Maranzan_printer.bin`
3. Commit e push su GitHub
4. Sul dispositivo: all'avvio appare menu boot (2s) → premi **pulsante SU** (IO39)
5. Il dispositivo scarica e installa automaticamente il nuovo firmware

**Comportamento:**
- Connette WiFi → scarica `.bin` da GitHub → progress bar → riavvio
- Se WiFi assente: mostra errore, avvio normale dopo 3s
- Se file non trovato su GitHub: mostra errore, avvio normale dopo 3s
- Se download fallisce: mostra errore HTTP, avvio normale dopo 3s

**Note:**
- Il file `.bin` deve essere pushato su branch `main`
- Dimensione max firmware: ~1.3MB (flash disponibile)
- L'update sovrascrive la partizione app corrente

---

## 🚀 ROADMAP: Migrazione a Cloudflare Workers + D1

### Obiettivo
Migrare il backend da Google Apps Script a Cloudflare Workers + D1 per:
- **Velocità:** Latenza 10-50ms (vs 200-500ms Google Apps Script)
- **Limiti:** 100.000 req/giorno gratuiti (vs 20.000 Google)
- **Polling T4:** Intervallo ridotto a 300-500ms (reattività quasi istantanea)

### Architettura Target

```
┌─────────────────────────────────────────────────────────────────────┐
│                        FLUSSO NORMALE (99%)                         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   Sito PWA ──────┐                                                  │
│                  │                                                  │
│                  ▼                                                  │
│            Cloudflare Worker ◄──── T4 Printer (polling 300-500ms)  │
│                  │                                                  │
│                  ▼                                                  │
│            D1 Database (SQLite)                                     │
│                  │                                                  │
│                  │ (async, ogni 30-60s)                             │
│                  ▼                                                  │
│            Google Sheets (backup + admin UI)                        │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                   FLUSSO INVERSO (modifiche manuali)                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   Admin modifica Google Sheets                                      │
│                  │                                                  │
│                  ▼                                                  │
│   Google Apps Script (trigger onEdit)                               │
│                  │                                                  │
│                  ▼                                                  │
│   Webhook → Cloudflare Worker                                       │
│                  │                                                  │
│                  ▼                                                  │
│   Aggiorna D1 Database                                              │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### Benefici Attesi

| Metrica | Attuale (Google) | Target (Cloudflare) |
|---------|------------------|---------------------|
| Latenza API | 200-500ms | 10-50ms |
| Polling T4 | 2.2s | 300-500ms |
| Tempo stampa da inserimento | ~8s | ~2s |
| Limite giornaliero | 20.000 | 100.000 |
| Cold start | 1-2s (occasionale) | 0ms |

### Schema Database D1

```sql
-- Tabella Riparazioni
CREATE TABLE riparazioni (
  numero TEXT PRIMARY KEY,           -- "26/0125"
  data_consegna DATE NOT NULL,
  data_completamento DATE,
  cliente TEXT NOT NULL,
  indirizzo TEXT,
  telefono TEXT,
  ddt INTEGER DEFAULT 0,             -- boolean
  completato INTEGER DEFAULT 0,      -- boolean
  attrezzi TEXT,                      -- JSON array
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- Tabella Clienti
CREATE TABLE clienti (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  nome TEXT NOT NULL,
  telefono TEXT,
  indirizzo TEXT,
  UNIQUE(nome, telefono)
);

-- Tabella Magazzino
CREATE TABLE magazzino (
  codice TEXT PRIMARY KEY,
  descrizione TEXT,
  scaffale TEXT
);

-- Tabella Sync (per tracciare ultima sincronizzazione con Sheets)
CREATE TABLE sync_log (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  direction TEXT,                     -- 'to_sheets' o 'from_sheets'
  table_name TEXT,
  record_id TEXT,
  timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- Indici per performance
CREATE INDEX idx_riparazioni_completato ON riparazioni(completato);
CREATE INDEX idx_riparazioni_data ON riparazioni(data_consegna);
CREATE INDEX idx_magazzino_scaffale ON magazzino(scaffale);
CREATE INDEX idx_clienti_nome ON clienti(nome);
```

### Endpoint API Cloudflare Worker

```
BASE_URL: https://api.elettromeccanica-maranzan.com (o workers.dev)

── RIPARAZIONI ──
GET  /api/riparazioni                  → Lista tutte (filtri: ?completato=false)
GET  /api/riparazioni/:numero          → Singola riparazione
GET  /api/riparazioni/next-numero      → Prossimo numero progressivo
POST /api/riparazioni                  → Crea nuova
PATCH /api/riparazioni/:numero         → Modifica esistente
POST /api/riparazioni/:numero/complete → Marca completata

── CLIENTI ──
GET  /api/clienti                      → Lista per autocomplete

── MAGAZZINO ──
GET  /api/ricambi                      → Lista completa
GET  /api/ricambi/:codice              → Singolo ricambio
POST /api/ricambi/batch                → Batch insert
PATCH /api/ricambi/:codice             → Modifica
DELETE /api/ricambi/:codice            → Elimina

── POLLING T4 ──
GET  /api/poll?ts=123456789            → Polling ottimizzato
     Risposta: { changed: bool, ts: number, command?: string, riparazione?: object }

── SYNC ──
POST /api/sync/from-sheets             → Webhook per sync da Sheets (trigger onEdit)
```

### Fasi Implementazione

#### Fase 1: Setup Infrastruttura
- [ ] Creare progetto Cloudflare Workers
- [ ] Creare database D1
- [ ] Configurare dominio custom (opzionale: api.elettromeccanica-maranzan.com)
- [ ] Setup Wrangler CLI per deploy

#### Fase 2: Migrazione Riparazioni
- [ ] Implementare Worker con tutti gli endpoint riparazioni
- [ ] Migrare dati esistenti da Sheets a D1
- [ ] Testare CRUD completo
- [ ] Aggiornare `js/riparazioni-*.js` per usare nuovo endpoint
- [ ] Testare in parallelo (switch graduale)

#### Fase 3: Migrazione Magazzino
- [ ] Implementare endpoint magazzino nel Worker
- [ ] Migrare dati da Sheets
- [ ] Aggiornare `js/magazzino-*.js`
- [ ] Testare batch insert

#### Fase 4: Sync Bidirezionale con Sheets
- [ ] Implementare sync D1 → Sheets (async, ogni 30-60s)
- [ ] Implementare trigger onEdit in Sheets → webhook Worker
- [ ] Testare modifiche manuali su Sheets

#### Fase 5: Aggiornamento T4 Printer
- [ ] Modificare `API_URL` nel firmware per puntare a Cloudflare
- [ ] Ridurre `POLL_FAST` a 300-500ms
- [ ] Testare latenza e affidabilità
- [ ] Deploy firmware via OTA

#### Fase 6: Cleanup e Ottimizzazioni
- [ ] Rimuovere dipendenza da Google Apps Script (mantenere solo per sync)
- [ ] Ottimizzare query D1 con indici
- [ ] Monitoraggio errori e performance
- [ ] Documentazione aggiornata

### File da Modificare

**Frontend (switch endpoint):**
```
js/riparazioni-nuovo.js      → riga 2: API_URL
js/riparazioni-archivio.js   → riga 2: API_URL
js/riparazioni-dettaglio.js  → riga 2: API_URL
js/magazzino-modifica.js     → riga 2: API_URL
js/magazzino-dettaglio.js    → riga 2: API_URL
```

**Firmware T4:**
```
hardware/t4-printer/src/main.cpp → riga 56: API_URL
                                 → riga 127-129: POLL_FAST (ridurre a 300-500ms)
```

**Nuovi file da creare:**
```
/cloudflare/
├── wrangler.toml              # Config Cloudflare
├── src/
│   ├── index.ts               # Entry point Worker
│   ├── routes/
│   │   ├── riparazioni.ts     # CRUD riparazioni
│   │   ├── magazzino.ts       # CRUD magazzino
│   │   ├── clienti.ts         # Lista clienti
│   │   └── poll.ts            # Polling T4
│   ├── db/
│   │   ├── schema.sql         # Schema D1
│   │   └── migrations/        # Migrazioni future
│   └── sync/
│       ├── to-sheets.ts       # Sync D1 → Sheets
│       └── from-sheets.ts     # Webhook Sheets → D1
└── README.md
```

**Google Apps Script (da modificare):**
```
gs/riparazioni.gs  → Aggiungere trigger onEdit → webhook Cloudflare
gs/magazzino.gs    → Aggiungere trigger onEdit → webhook Cloudflare
```

### Note Importanti

1. **Transizione graduale:** Mantenere Google Apps Script funzionante durante la migrazione. Switch atomico solo quando tutto testato.

2. **Consistenza dati:** Il sync bidirezionale può creare conflitti. Regola: D1 vince sempre, Sheets è "eventualmente consistente".

3. **Fallback:** Se Cloudflare ha problemi, il sito può temporaneamente tornare a Google Apps Script (cambiando API_URL).

4. **Costi:** Cloudflare Workers free tier è generoso (100k req/giorno, 10ms CPU/req). D1 free: 5GB storage, 5M rows read/day.

5. **CORS:** Il Worker deve gestire CORS per richieste dal browser (`Access-Control-Allow-Origin`).

6. **Autenticazione:** Considerare API key o altro meccanismo per proteggere endpoint POST/PATCH/DELETE.

### Stima Effort

| Fase | Complessità | Tempo stimato |
|------|-------------|---------------|
| Fase 1: Setup | Bassa | 2-3 ore |
| Fase 2: Riparazioni | Media | 4-6 ore |
| Fase 3: Magazzino | Media | 3-4 ore |
| Fase 4: Sync | Alta | 4-6 ore |
| Fase 5: T4 | Bassa | 1-2 ore |
| Fase 6: Cleanup | Bassa | 2-3 ore |
| **Totale** | | **16-24 ore** |

---

## 📚 File Importanti da Non Toccare

- `/manifest.webmanifest` - PWA config (cambiare `start_url` richiede reinstall app)
- `/gs/*.gs` - Google Apps Script (deploy manuale su Google)

---